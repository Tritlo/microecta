# Future work
+ Generate from arbitrary ECTAs with direct counting and unranking of finite equality-constrained automata, eventually extending that boundary to bounded recursive languages.
+ Term-level shrink rewrites validated by nodeRepresents against the support: propose subtree replacements from the support's own edges and let the automaton check acceptance, complementing the delivered size-ordered minimal search. Size-stratified counting per plan node (FEAT-style convolution) would additionally allow jumping directly into a size class by index.
